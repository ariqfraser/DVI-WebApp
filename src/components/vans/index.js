import React from 'react'

import {Container, SectionTitle, SectoionP, SplitL,SplitR, AboutImg} from './vans.elements'
import {SmallLine} from '../line.elements'

const Vans = () => {
    return (
        <>
            <Container id='about'>
                <SplitL>
                    
                </SplitL>
                <SplitR>
                    <SectionTitle>All New Ice Cream Vans!</SectionTitle>

                    <SectoionP>
                    We have recently decided to expand across the UK and want to maintain our local, family feel and nature.  This is not a small undertaking and we need to get it right first time.  

                    </SectoionP>
                </SplitR>
            </Container>
        </>
    )
}

export default Vans
